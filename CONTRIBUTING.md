# Contributing

このリポジトリへの貢献を検討いただきありがとうございます。

## 開発の始め方

```bash
git clone https://github.com/lazytomato17-ops/tomatobot.git
cd tomatobot
npm install
cp .env.example .env   # 各種トークン・APIキーを設定
npm run dev
```

## ブランチ運用

- `main` ブランチへの直接 push は避け、Pull Request 経由でマージしてください。
- ブランチ名は `feature/役職を追加する` のように、変更内容が分かる名前を推奨します。

## コミット前のチェック

Pull Request を作成する前に、以下をローカルで通過させてください（CIでも同じチェックが走ります）。

```bash
npm run typecheck
npm run lint
npm run build
```

新規・変更したファイルについては、可能であれば以下も実行してください。

```bash
npx prettier --write <変更したファイル>
```

> 既存コードはあえて Prettier 未整形のまま残しています。`npm run format` をリポジトリ全体に対して実行すると、既存コードがほぼ全行書き換わる巨大な差分になりレビューが困難になるため、**新規・変更ファイルにのみ**適用してください。

## Pull Request を出す際のお願い

- 変更の目的・背景を簡潔に説明してください。
- ゲームバランスに関わる変更（役職の能力・確率調整など）は、変更前後の挙動の違いを明記してください。
- `src/phase/core.ts` に新しいフェーズ処理を追加する場合は、既存のフェーズ関数同士の呼び出し関係（昼→投票→夜→朝→昼…）を崩さないよう注意してください。循環import を避けるため、このファイルへの追加は同ファイル内で完結させることを推奨します。

## バグ報告

Issue を作成する際は、以下の情報があると調査がスムーズです。

- 発生した状況（例: ランクマッチで特定の役職を選んだ際の挙動）
- 期待していた動作と、実際の動作
- 可能であればログやエラーメッセージ
